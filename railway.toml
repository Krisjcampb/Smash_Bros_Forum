# Tells Railway how to build and start your server

[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "node dataconnect.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3